import { Typography } from '@mui/material';
import { NextPage } from 'next';

const Home: NextPage = () => (
  <div>
    <Typography variant="h1">Hello</Typography>
    Hello
  </div>
);

export default Home;
